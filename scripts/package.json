{
  "name": "scripts",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "npm run build && npm run test",
    "build": "node build",
    "test": "node test"
  },
  "license": "ISC",
  "dependencies": {
    "chalk": "^2.3.1",
    "shelljs": "^0.8.1"
  },
  "devDependencies": {
    "newman": "^4.2.2"
  }
}
